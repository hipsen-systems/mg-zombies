# scripts/

Temporary folder used to verify the PR enforcement pipeline (docs-check CI
and the Stop hook). Contains only `enforcement_test.gd`, a no-op Node script
with no gameplay purpose. This folder is deleted when the verification PR
closes.
